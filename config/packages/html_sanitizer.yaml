framework:
    html_sanitizer:
        sanitizers:
            default:
                allow_safe_elements: true
            notification:
                allow_safe_elements: true
                allow_elements:
                    callout: '*'
            video:
                allow_safe_elements: true
                allow_elements:
                    iframe: ['src', 'width', 'height', 'frameborder', 'allow', 'allowfullscreen', 'title']
                    div: ['class', 'style']
                    p: '*'
                    br: '*'

{% if app.user %}
	{% set completionPercent = completion_percentage(course, app.user) %}
	<ion-progress-bar value="{{ completionPercent / 100 }}" color="app" buffer="0.01">
		{{completionPercent}}
		%
	</ion-progress-bar>
{% endif %}

<ion-card>
	<img alt="{{'Course poster' | trans}}" src="{{asset(course.author.image)}}"/>
	<ion-card-header>
		<ion-card-title>{{'Your teacher' | trans}}</ion-card-title>
		<ion-card-subtitle>
			{{course.author.fullname}}
		</ion-card-subtitle>
	</ion-card-header>
	<ion-card-content>
		{{course.author.bio}}
	</ion-card-content>
</ion-card>

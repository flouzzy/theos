<ion-list>
	{% for lesson in module.lessons %}
		<ion-item href="{{path('lesson_show', {courseSlug:course.slug, moduleSlug:module.slug, id:lesson.id})}}">
			{% set completedIcon = lesson.completed ? 'checkmark-circle': 'ellipse' %}
			<ion-icon slot="start" name="{{completedIcon}}-outline"></ion-icon>
			<ion-label>{{lesson.title}}</ion-label>
			<ion-button slot="end" color="danger">
				{{'Preview' | trans}}
			</ion-button>
		</ion-item>
	{% endfor %}
</ion-list>

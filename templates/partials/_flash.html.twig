{% for label, messages in app.flashes %}
	{% for message in messages %}
		<ion-toast trigger="open-toast" position="bottom" color="{{label}}" message="{{ message }}" is-open="true" duration="7000"></ion-toast>
	{% endfor %}
{% endfor %}

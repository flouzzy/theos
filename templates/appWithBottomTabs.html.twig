{% extends 'base.html.twig' %}

{% block body %}
<div class="min-h-screen bg-background pb-16 md:pb-0" x-data="{ mobileMenuOpen: false, moreMenuOpen: false, desktopSidebarOpen: true }">
    
    {# Desktop Sidebar #}
    <aside class="hidden lg:fixed lg:inset-y-0 lg:left-0 lg:top-0 lg:bottom-0 lg:flex lg:flex-col lg:w-64 bg-sidebar border-r border-border transition-all duration-300 z-40" 
           :class="desktopSidebarOpen ? 'lg:w-64' : 'lg:w-20'">
        <div class="flex grow flex-col gap-y-5 overflow-y-auto px-6 py-6" :class="!desktopSidebarOpen && 'px-3'">
            {# Logo #}
            <div class="flex h-16 shrink-0 items-center justify-between">
                <div class="flex items-center gap-3 overflow-hidden">
                    <div class="h-10 w-10 shrink-0 rounded-xl bg-primary flex items-center justify-center text-white font-bold text-lg shadow-lg shadow-primary/20">
                        🦉
                    </div>
                    <div class="transition-opacity duration-200" :class="!desktopSidebarOpen && 'opacity-0 w-0'">
                        <span class="text-lg font-bold tracking-tight text-foreground">Le Rocher</span>
                        <span class="block text-xs font-medium text-muted-foreground">Académie</span>
                    </div>
                </div>
                <button @click="desktopSidebarOpen = !desktopSidebarOpen" class="hidden lg:flex p-1.5 rounded-lg text-muted-foreground hover:bg-muted hover:text-foreground transition-colors">
                    <svg class="w-5 h-5 transition-transform duration-300" :class="!desktopSidebarOpen && 'rotate-180'" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 19l-7-7 7-7m8 14l-7-7 7-7" />
                    </svg>
                </button>
            </div>

            {# Navigation #}
            {% include 'partials/_sidebar_navigation.html.twig' %}

            {# User Profile Footer #}
            <div class="mt-auto pb-4">
                {% if app.user %}
                    <div class="flex items-center gap-3 px-3 py-2 border-t border-border pt-4 overflow-hidden">
                        <div class="h-10 w-10 shrink-0 rounded-full bg-primary text-primary-foreground flex items-center justify-center font-semibold shadow-sm">
                            {{ app.user.firstname|first }}{{ app.user.lastname|first }}
                        </div>
                        <div class="flex-1 min-w-0 transition-opacity duration-200" :class="!desktopSidebarOpen && 'opacity-0 w-0'">
                            <p class="text-sm font-semibold truncate">{{ app.user.firstname }} {{ app.user.lastname }}</p>
                            {% set activeCohort = active_cohort() %}
                            {% if activeCohort %}
                                <p class="text-xs text-muted-foreground truncate">{{ activeCohort.title }}</p>
                            {% endif %}

                        </div>
                    </div>
                    <a href="{{ path('_logout_main') }}" class="mt-2 flex items-center gap-2 px-3 py-2 text-sm text-muted-foreground hover:text-foreground transition-colors">
                        <svg class="h-4 w-4 shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
                        </svg>
                        <span :class="!desktopSidebarOpen && 'hidden'">{{ 'Déconnexion' | trans }}</span>
                    </a>
                {% endif %}
            </div>
        </div>
    </aside>

    {# Mobile Menu Overlay & Drawer #}
    <div x-show="moreMenuOpen" 
         class="fixed inset-0 z-50 lg:hidden"
         style="display: none;">
         
         {# Backdrop #}
         <div x-transition:enter="transition-opacity ease-linear duration-300"
              x-transition:enter-start="opacity-0"
              x-transition:enter-end="opacity-100"
              x-transition:leave="transition-opacity ease-linear duration-300"
              x-transition:leave-start="opacity-100"
              x-transition:leave-end="opacity-0"
              @click="moreMenuOpen = false"
              class="absolute inset-0 bg-black/60 backdrop-blur-sm">
         </div>

         {# Drawer #}
         <div x-transition:enter="transition ease-out duration-300 transform"
              x-transition:enter-start="translate-y-full"
              x-transition:enter-end="translate-y-0"
              x-transition:leave="transition ease-in duration-300 transform"
              x-transition:leave-start="translate-y-0"
              x-transition:leave-end="translate-y-full"
              class="absolute bottom-0 left-0 right-0 bg-background rounded-t-3xl shadow-2xl max-h-[85vh] overflow-y-auto w-full">
              
              <div class="p-6 space-y-6">
                   {# Handle #}
                   <div class="w-12 h-1.5 bg-muted-foreground/20 rounded-full mx-auto mb-2"></div>
                   
                   <div class="text-center mb-6">
                        <h2 class="text-xl font-bold">{{ 'Menu Principal' | trans }}</h2>
                   </div>

                   <div class="grid grid-cols-2 gap-4">
                        {# Notes #}
                        <a href="{{ path('note_index') }}" class="bg-yellow-50 hover:bg-yellow-100 border-2 border-yellow-100 dark:border-yellow-900/50 p-4 rounded-2xl flex flex-col items-center gap-3 transition-colors">
                            <div class="w-12 h-12 bg-yellow-100 text-yellow-600 rounded-full flex items-center justify-center">
                                <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
                                </svg>
                            </div>
                            <span class="font-bold text-gray-700">{{ 'Bloc-notes' | trans }}</span>
                        </a>

                        {# Evaluations #}
                        <a href="{{ path('evaluation_index') }}" class="bg-blue-50 hover:bg-blue-100 border-2 border-blue-100 dark:border-blue-900/50 p-4 rounded-2xl flex flex-col items-center gap-3 transition-colors">
                            <div class="w-12 h-12 bg-blue-100 text-blue-600 rounded-full flex items-center justify-center">
                                <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
                                </svg>
                            </div>
                            <span class="font-bold text-gray-700">{{ 'Évaluations' | trans }}</span>
                        </a>
                        
                        {# Leaderboard #}
                        <a href="{{ path('leaderboard_index') }}" class="bg-indigo-50 hover:bg-indigo-100 border-2 border-indigo-100 dark:border-indigo-900/50 p-4 rounded-2xl flex flex-col items-center gap-3 transition-colors">
                            <div class="w-12 h-12 bg-indigo-100 text-indigo-600 rounded-full flex items-center justify-center">
                                <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6" />
                                </svg>
                            </div>
                            <span class="font-bold text-gray-700">{{ 'Classement' | trans }}</span>
                        </a>

                        {# Admin (If Admin) #}
                        {% if is_granted('ROLE_ADMIN') %}
                         <a href="{{ path('admin') }}" class="bg-gray-50 hover:bg-gray-100 border-2 border-gray-100 dark:border-gray-800 p-4 rounded-2xl flex flex-col items-center gap-3 transition-colors">
                            <div class="w-12 h-12 bg-gray-200 text-gray-700 rounded-full flex items-center justify-center">
                                <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                                </svg>
                            </div>
                            <span class="font-bold text-gray-700">{{ 'Admin' | trans }}</span>
                        </a>
                        {% endif %}
                   </div>
                   
                   <div class="mt-8 pt-4 border-t border-border">
                        <button @click="moreMenuOpen = false" class="w-full btn bg-muted text-foreground font-bold rounded-xl py-3">
                            {{ 'Fermer' | trans }}
                        </button>
                   </div>
              </div>
         </div>
    </div>

    {# Main Content Area with Dynamic Padding for Sidebar #}
    <div class="lg:pl-64 transition-all duration-300 flex flex-col min-h-screen" 
         :class="desktopSidebarOpen ? 'lg:pl-64' : 'lg:pl-20'">
        
        {# Unified Sticky Toolbar (Desktop & Mobile) #}
        <header class="sticky top-0 z-30 flex h-16 items-center gap-x-4 border-b border-border bg-background/80 backdrop-blur-md px-4 lg:px-8 shadow-sm transition-all duration-300">
            
            {# Center/Left: Page Title #}
            <div class="flex-1 min-w-0 flex items-center gap-2">
                <h1 class="text-base lg:text-lg font-bold truncate">
                    {% block page_title %}{{ 'Le Rocher Académie' | trans }}{% endblock %}
                </h1>
            </div>

            {# Right: Actions & Stats #}
            <div class="flex items-center gap-3">
                 {% block toolbar_right %}
                    <div class="flex items-center gap-1 bg-orange-100 text-orange-600 px-2.5 py-1 rounded-full text-xs font-bold ring-1 ring-orange-200">
                        <span>🔥</span> 12
                    </div>
                 {% endblock %}
            </div>
        </header>

        {# Page Content #}
        <main class="py-6 px-4 lg:px-8 flex-1">
            <div class="max-w-7xl mx-auto">
                {% include "partials/_flash.html.twig" %}
                {% block content %}{% endblock %}
            </div>
        </main>
    </div>

    {# Mobile Bottom Navigation (Fixed) #}
    <nav class="fixed bottom-0 left-0 right-0 z-40 bg-white/95 backdrop-blur-md border-t border-border lg:hidden pb-safe">
        <div class="flex justify-around items-center h-16 px-1">
            {# Tab 1: Dashboard #}
            <a href="{{ path('cohort_index') }}" class="tab-item {{ app.request.attributes.get('_route') == 'cohort_index' ? 'active text-primary' : '' }} flex-1 flex flex-col items-center justify-center gap-1 group">
                <div class="p-1 rounded-xl transition-all {{ app.request.attributes.get('_route') == 'cohort_index' ? 'bg-primary/10' : '' }}">
                    <svg class="h-6 w-6 transition-transform group-active:scale-95" fill="{{ app.request.attributes.get('_route') == 'cohort_index' ? 'currentColor' : 'none' }}" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
                    </svg>
                </div>
                <span class="text-[10px] font-bold">{{ 'Accueil' | trans }}</span>
            </a>
            
            {# Tab 2: Cours #}
            <a href="{{ path('course_index') }}" class="tab-item {{ app.request.attributes.get('_route') starts with 'course' ? 'active text-primary' : '' }} flex-1 flex flex-col items-center justify-center gap-1 group">
                <div class="p-1 rounded-xl transition-all {{ app.request.attributes.get('_route') starts with 'course' ? 'bg-primary/10' : '' }}">
                   <svg class="h-6 w-6 transition-transform group-active:scale-95" fill="{{ app.request.attributes.get('_route') starts with 'course' ? 'currentColor' : 'none' }}" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" />
                    </svg>
                </div>
                <span class="text-[10px] font-bold">{{ 'Cours' | trans }}</span>
            </a>
            
            {# Tab 3: Coach #}
            <a href="{{ path('coach_index') }}" class="tab-item {{ app.request.attributes.get('_route') starts with 'coach' ? 'active text-primary' : '' }} flex-1 flex flex-col items-center justify-center gap-1 group">
                <div class="p-1 rounded-xl transition-all {{ app.request.attributes.get('_route') starts with 'coach' ? 'bg-primary/10' : '' }}">
                     <svg class="h-6 w-6 transition-transform group-active:scale-95" fill="{{ app.request.attributes.get('_route') starts with 'coach' ? 'currentColor' : 'none' }}" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z" />
                    </svg>
                </div>
                <span class="text-[10px] font-bold">{{ 'Coach' | trans }}</span>
            </a>
            
            {# Tab 4: Profil #}
            <a href="{{ path('profile_index') }}" class="tab-item {{ app.request.attributes.get('_route') starts with 'profile' ? 'active text-primary' : '' }} flex-1 flex flex-col items-center justify-center gap-1 group">
                <div class="p-1 rounded-xl transition-all {{ app.request.attributes.get('_route') starts with 'profile' ? 'bg-primary/10' : '' }}">
                     <svg class="h-6 w-6 transition-transform group-active:scale-95" fill="{{ app.request.attributes.get('_route') starts with 'profile' ? 'currentColor' : 'none' }}" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                    </svg>
                </div>
                <span class="text-[10px] font-bold">{{ 'Profil' | trans }}</span>
            </a>
            
            {# Tab 5: Plus (Opens drawer menu) #}
            <button @click="moreMenuOpen = !moreMenuOpen" class="tab-item flex-1 flex flex-col items-center justify-center gap-1 group text-muted-foreground hover:text-foreground">
                <div class="p-1 rounded-xl transition-all" :class="{'bg-muted': moreMenuOpen}">
                    <svg class="h-6 w-6 transition-transform group-active:scale-95" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                    </svg>
                </div>
                <span class="text-[10px] font-bold">{{ 'Plus' | trans }}</span>
            </button>
        </div>
    </nav>
</div>
{% endblock %}

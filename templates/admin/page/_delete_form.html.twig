<ion-item>
	<form method="post" action="{{ path('admin_page_delete', {'id': page.id}) }}" onsubmit="return confirm('{{ 'Êtes-vous sûr de vouloir supprimer cette page ?'|trans }}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ page.id) }}">
    <twig:Button variant="destructive" class="w-full">
        <twig:ux:icon name="lucide:trash-2" class="w-4 h-4 mr-2" />
        {{ 'Supprimer' | trans }}
    </twig:Button>
</form>
</ion-item>

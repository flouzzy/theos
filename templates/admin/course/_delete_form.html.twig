<ion-item>
	<form method="post" action="{{ path('admin_course_delete', {'id': course.id}) }}" onsubmit="return confirm('{{'Are you sure you want to delete this item ?' | trans}}');">
		<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ course.id) }}">
		<ion-button color="danger" type="submit">{{'Delete' | trans}}</ion-button>
	</form>
</ion-item>

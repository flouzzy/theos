{{ form_start(form) }}
<ion-list>
	<ion-item>
		{{ form_widget(form) }}
	</ion-item>
	<ion-item lines="none">
		<ion-button color="success" size="normal" type="submit">{{ button_label|default('Save')|trans }}</ion-button>
	</ion-item>
</ion-list>
{{ form_end(form) }}

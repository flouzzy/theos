<div class="px-3 py-2 space-y-1">
    <h3 class="px-2 text-xs font-semibold text-slate-500 uppercase tracking-wider mb-2">
        {{ 'General' | trans }}
    </h3>
    <a href="{{ path('admin') }}" class="flex items-center gap-3 px-3 py-2 text-sm font-medium rounded-lg transition-colors {% if currentroute == 'admin' %}bg-slate-800 text-white{% else %}text-slate-400 hover:bg-slate-800/50 hover:text-white{% endif %}">
        <twig:ux:icon name="lucide:layout-dashboard" class="w-5 h-5" />
        {{ 'Dashboard' | trans }}
    </a>
    <a href="{{ path('admin_cohort_index') }}" class="flex items-center gap-3 px-3 py-2 text-sm font-medium rounded-lg transition-colors {% if currentroute == 'admin_cohort_index' %}bg-slate-800 text-white{% else %}text-slate-400 hover:bg-slate-800/50 hover:text-white{% endif %}">
        <twig:ux:icon name="lucide:users" class="w-5 h-5" />
        {{ 'Cohorts' | trans }}
    </a>
    <a href="{{ path('admin_calendar_index') }}" class="flex items-center gap-3 px-3 py-2 text-sm font-medium rounded-lg transition-colors {% if currentroute starts with 'admin_calendar' %}bg-slate-800 text-white{% else %}text-slate-400 hover:bg-slate-800/50 hover:text-white{% endif %}">
        <twig:ux:icon name="lucide:calendar" class="w-5 h-5" />
        {{ 'Calendriers' | trans }}
    </a>
    <a href="{{ path('admin_course_index') }}" class="flex items-center gap-3 px-3 py-2 text-sm font-medium rounded-lg transition-colors {% if currentroute == 'admin_course_index' %}bg-slate-800 text-white{% else %}text-slate-400 hover:bg-slate-800/50 hover:text-white{% endif %}">
        <twig:ux:icon name="lucide:book-open" class="w-5 h-5" />
        {{ 'Courses' | trans }}
    </a>
    <a href="{{ path('admin_module_index') }}" class="flex items-center gap-3 px-3 py-2 text-sm font-medium rounded-lg transition-colors {% if currentroute == 'admin_module_index' %}bg-slate-800 text-white{% else %}text-slate-400 hover:bg-slate-800/50 hover:text-white{% endif %}">
        <twig:ux:icon name="lucide:layers" class="w-5 h-5" />
        {{ 'Modules' | trans }}
    </a>
    <a href="{{ path('admin_lesson_index') }}" class="flex items-center gap-3 px-3 py-2 text-sm font-medium rounded-lg transition-colors {% if currentroute == 'admin_lesson_index' %}bg-slate-800 text-white{% else %}text-slate-400 hover:bg-slate-800/50 hover:text-white{% endif %}">
        <twig:ux:icon name="lucide:file-text" class="w-5 h-5" />
        {{ 'Lessons' | trans }}
    </a>
</div>

<div class="px-3 py-2 mt-4 space-y-1">
    <h3 class="px-2 text-xs font-semibold text-slate-500 uppercase tracking-wider mb-2">
        {{ 'Activity' | trans }}
    </h3>
    <a href="{{ path('admin_notification_index') }}" class="flex items-center gap-3 px-3 py-2 text-sm font-medium rounded-lg transition-colors {% if currentroute == 'admin_notification_index' %}bg-slate-800 text-white{% else %}text-slate-400 hover:bg-slate-800/50 hover:text-white{% endif %}">
        <twig:ux:icon name="lucide:bell" class="w-5 h-5" />
        {{ 'Notifications' | trans }}
    </a>
    <a href="{{ path('admin_note_index') }}" class="flex items-center gap-3 px-3 py-2 text-sm font-medium rounded-lg transition-colors {% if currentroute == 'admin_note_index' %}bg-slate-800 text-white{% else %}text-slate-400 hover:bg-slate-800/50 hover:text-white{% endif %}">
        <twig:ux:icon name="lucide:bookmark" class="w-5 h-5" />
        {{ 'Notes' | trans }}
    </a>
    <a href="{{ path('admin_completion_index') }}" class="flex items-center gap-3 px-3 py-2 text-sm font-medium rounded-lg transition-colors {% if currentroute == 'admin_completion_index' %}bg-slate-800 text-white{% else %}text-slate-400 hover:bg-slate-800/50 hover:text-white{% endif %}">
        <twig:ux:icon name="lucide:check-circle" class="w-5 h-5" />
        {{ 'Completion status' | trans }}
    </a>
    <a href="{{ path('app_admin_evaluation_index') }}" class="flex items-center gap-3 px-3 py-2 text-sm font-medium rounded-lg transition-colors {% if currentroute == 'app_admin_evaluation_index' %}bg-slate-800 text-white{% else %}text-slate-400 hover:bg-slate-800/50 hover:text-white{% endif %}">
        <twig:ux:icon name="lucide:clipboard-check" class="w-5 h-5" />
        {{ 'Evaluations' | trans }}
    </a>
    <a href="{{ path('admin_badge_type_index') }}" class="flex items-center gap-3 px-3 py-2 text-sm font-medium rounded-lg transition-colors {% if currentroute == 'admin_badge_type_index' %}bg-slate-800 text-white{% else %}text-slate-400 hover:bg-slate-800/50 hover:text-white{% endif %}">
        <twig:ux:icon name="lucide:award" class="w-5 h-5" />
        {{ 'Badge' | trans }}
    </a>
</div>

<div class="px-3 py-2 mt-4 space-y-1">
    <h3 class="px-2 text-xs font-semibold text-slate-500 uppercase tracking-wider mb-2">
        {{ 'Settings' | trans }}
    </h3>
    <a href="{{ path('admin_page_index') }}" class="flex items-center gap-3 px-3 py-2 text-sm font-medium rounded-lg transition-colors {% if currentroute == 'admin_page_index' %}bg-slate-800 text-white{% else %}text-slate-400 hover:bg-slate-800/50 hover:text-white{% endif %}">
        <twig:ux:icon name="lucide:file-code" class="w-5 h-5" />
        {{ 'Pages' | trans }}
    </a>
    <a href="{{ path('admin_user_index') }}" class="flex items-center gap-3 px-3 py-2 text-sm font-medium rounded-lg transition-colors {% if currentroute == 'admin_user_index' %}bg-slate-800 text-white{% else %}text-slate-400 hover:bg-slate-800/50 hover:text-white{% endif %}">
        <twig:ux:icon name="lucide:users-2" class="w-5 h-5" />
        {{ 'Users' | trans }}
    </a>
    <a href="{{ path('admin_payment_index') }}" class="flex items-center gap-3 px-3 py-2 text-sm font-medium rounded-lg transition-colors {% if currentroute == 'admin_payment_index' %}bg-slate-800 text-white{% else %}text-slate-400 hover:bg-slate-800/50 hover:text-white{% endif %}">
        <twig:ux:icon name="lucide:credit-card" class="w-5 h-5" />
        {{ 'Payment settings' | trans }}
    </a>
    <a href="{{ path('admin_setting_index') }}" class="flex items-center gap-3 px-3 py-2 text-sm font-medium rounded-lg transition-colors {% if currentroute == 'admin_setting_index' %}bg-slate-800 text-white{% else %}text-slate-400 hover:bg-slate-800/50 hover:text-white{% endif %}">
        <twig:ux:icon name="lucide:settings" class="w-5 h-5" />
        {{ 'Paramètres' | trans }}
    </a>
</div>
<div class="px-3 py-2 mt-4 space-y-1">
    <h3 class="px-2 text-xs font-semibold text-slate-500 uppercase tracking-wider mb-2">
        {{ 'Account' | trans }}
    </h3>
    <a href="{{ path('profile_index') }}" class="flex items-center gap-3 px-3 py-2 text-sm font-medium rounded-lg transition-colors {% if currentroute == 'profile_index' %}bg-slate-800 text-white{% else %}text-slate-400 hover:bg-slate-800/50 hover:text-white{% endif %}">
        <twig:ux:icon name="lucide:user-circle" class="w-5 h-5" />
        {{ 'Profile' | trans }}
    </a>
</div>

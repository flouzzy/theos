{% extends 'app.html.twig' %}

{% block title %}
	{{'Notifications' | trans}}
{% endblock %}

{% block content %}
	<div class="max-w-4xl mx-auto">
		{{ component('NotificationList') }}
	</div>
{% endblock %}

{% block menuBackButton %}
	<a href="{{path('cohort_index')}}" class="flex items-center gap-2 text-muted-foreground hover:text-foreground transition-colors px-4 py-2">
		<twig:ux:icon name="lucide:chevron-left" class="w-5 h-5" />
        <span class="font-medium">{{ 'Retour' | trans }}</span>
	</a>
{% endblock %}